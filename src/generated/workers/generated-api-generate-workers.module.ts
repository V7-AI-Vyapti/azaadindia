import { Module } from '@nestjs/common';

/**
 * API-generate worker modules register here via project generation codegen.
 * Extend `imports` when async api-generate tools are configured.
 */
@Module({
    imports: [],
})
export class GeneratedApiGenerateWorkersModule {}
