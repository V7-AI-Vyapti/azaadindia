import { Module } from '@nestjs/common';

/**
 * API-generate modules register here via project generation codegen.
 * Extend `imports` when api-generate tools are configured.
 */
@Module({
    imports: [],
})
export class GeneratedApiGenerateApiModule {}
